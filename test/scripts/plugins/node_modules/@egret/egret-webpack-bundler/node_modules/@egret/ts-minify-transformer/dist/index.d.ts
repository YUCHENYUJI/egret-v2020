import * as ts from "typescript";
export declare type TransformOptions = {
    mode: "debug" | "release";
};
export declare const myTransformer: (program: ts.Program, options: TransformOptions) => (context: ts.TransformationContext) => (sf: ts.SourceFile) => ts.SourceFile;
