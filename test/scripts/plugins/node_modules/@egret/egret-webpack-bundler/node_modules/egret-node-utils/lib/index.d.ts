import * as fs from 'fs-extra-promise';
export declare type WalkFilter = (path: string, stats?: fs.Stats) => boolean;
export declare namespace walk {
    interface WalkOptions {
        relative?: boolean;
        ignoreHiddenFile?: boolean;
    }
}
export declare function walk(path: string | string[], filter: WalkFilter, options?: walk.WalkOptions): Promise<string[]>;
import * as cp from 'child_process';
export declare var shell: (path: string, args: string[], opt?: cp.SpawnOptions, verbase?: boolean) => Promise<number>;
export declare var sleep: (time: any) => Promise<{}>;
